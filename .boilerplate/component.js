// @flow
import * as React from "react";

import foo from "";

type propTypes = {
	users: Array<Object>
};

/**
 * @pure: ?true
 * @hasTests: false
 */
export function Name(props: propTypes): React.Node {
	return (
		<>
			<div>{arg}</div>
		</>
	);
}
